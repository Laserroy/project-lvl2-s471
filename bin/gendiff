#!/usr/bin/env php
<?php

namespace DiffGenerator;
use function DiffGenerator\run;
use function DiffGenerator\Cli\runCommandLineActions;

$autoloadPath1 = __DIR__ . '/../../../autoload.php';
$autoloadPath2 = __DIR__ . '/../vendor/autoload.php';
if (file_exists($autoloadPath1)) {
    require $autoloadPath1;
} else {
    require $autoloadPath2;
}
$docDescription = <<<'DOCOPT'
        Example of program which uses [options] shortcut in pattern.
        Usage:
            gendiff (-h | --help)
            gendiff --version
            gendiff [--format=TYPE] PATH1 PATH2

        Arguments:
            PATH1
            PATH2
            TYPE
        Options:
            -h --help                 Show this help message and exit
            --version                 Show version.
            --format=TYPE             Show difference in selected type.
DOCOPT;

$params = array(
    'help' => true,
    'version' => 'v0.0.1',
);

runCommandLineActions($docDescription, $params);

